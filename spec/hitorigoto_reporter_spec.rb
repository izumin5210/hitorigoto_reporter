require 'spec_helper'

describe HitorigotoReporter do
  it 'has a version number' do
    expect(HitorigotoReporter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
